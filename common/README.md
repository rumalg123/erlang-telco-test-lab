# Shared libraries

Status: scaffolded.

Only protocol codecs, signaling endpoint contracts, scenario event schemas,
and test utilities used by multiple components belong here. Component-specific
state or business behavior must stay in its owning directory.

