[
    {telco_stp, [
        %% Replace the example secret through the deployment's secret
        %% delivery mechanism. Do not commit an operational value.
        {ha, #{
            mode => primary,
            peers => ['stp_b@standby-host'],
            interval_ms => 1000,
            replication_timeout_ms => 2000,
            max_staleness_ms => 10000,
            max_clock_skew_ms => 5000,
            shared_secret =>
                <<"REPLACE-WITH-32-BYTE-SHARED-SECRET">>,
            fencing_token_sha256 => undefined,
            snapshot_path => undefined
        }}
    ]}
].
